#pragma once
#include <vertex.hpp>

    vertex::vertex(const double& len_x = 0.0, const double& len_y = 0.0, const double& len_z = 0.0) : vector(len_x, len_y, len_z)
    {
    }

    vertex::~vertex()
    {
    }